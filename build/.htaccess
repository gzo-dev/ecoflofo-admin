<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Xác định một số quy tắc cho routing của ReactJS
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
